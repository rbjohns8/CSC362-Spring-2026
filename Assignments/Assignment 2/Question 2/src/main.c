#include <stdio.h>
#include "utils.h"
#include "../include/math_lib.h"

int main() {
    int result = multiply(3, 4);  // Call the multiply function from the lib
    print_message("src: Main program executed");
    return 0;
}
